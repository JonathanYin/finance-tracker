# Plaid API credentials.
# Start with sandbox values from the Plaid dashboard.
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Local encryption key for provider access tokens.
# Generate with: openssl rand -base64 32
FINANCE_TRACKER_SECRET=
